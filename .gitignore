.gradle/
build/
*.log

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store

# Demo
demo/node_modules/
demo/dist/
demo/src/kotlin/

# ML-Modelle sind NICHT Teil der Lib (Konsument reicht sie zur Laufzeit rein)
*.onnx
*.pt

# Parity-Test: lokale venv (falls angelegt)
comic-cutter-onnx-jvm/parity/.venv/

# Claude Code lokale Projekt-State (Marker, settings.local) — maschinenspezifisch
.claude/

# Projekt-lokale Claude-Konventionen (gitignored auf Wunsch)
/CLAUDE.md

# Moved YOLO training pipeline: keep scripts/configs, ignore heavy artefacts (models already via *.pt/*.onnx)
training/dataset/
training/runs/
training/models/
training/base/
**/__pycache__/
