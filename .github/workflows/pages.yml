name: Deploy Demo to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Aktiviert GitHub Pages beim ersten Lauf via GITHUB_TOKEN (pages: write),
      # ohne manuelle Repo-Einstellung.
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Kotlin/JS-Bundle bauen
        run: ./gradlew jsBrowserDistribution --no-daemon

      - name: Bundle in die Demo kopieren
        run: |
          mkdir -p demo/src/kotlin
          cp build/dist/js/productionExecutable/comic-cutter.js demo/src/kotlin/
          cp build/dist/js/productionExecutable/comic-cutter.js.map demo/src/kotlin/ 2>/dev/null || true

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Demo bauen
        working-directory: demo
        run: |
          npm ci
          npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: demo/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
